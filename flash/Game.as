package  {	import flash.events.Event;	import flash.events.MouseEvent;		import flash.display.MovieClip;	import flash.net.SharedObject;	import fl.controls.Button;			public class Game extends MovieClip {		// players		private var player:Person = new Person();		private var challenger:Person = new Person();		private var so:SharedObject = SharedObject.getLocal("boutwar_userdata03","/");				private var bouts = Array[Bout];		private var numberOfRounds:int = 4;				//Forms		private var loginForm:LoginForm;		public function Game() {			// constructor code			loadGameData();			logUserIn();		}				private function logUserIn(){						if (!so.data.userName){				// display login form				// addChild				loginForm = new LoginForm();				loginForm.name = 'startForm';				addChild(loginForm);							} else {				userLoggedIn();				var returnForm:ReturnForm = new ReturnForm();				returnForm.name = 'startForm';				addChild(returnForm);				}					}				public function logInUser(p:Person){			trace('got a user at main: ' + p.getName());		}				private function userLoggedIn(){			// if login form child not null, remove child			trace('user logged in');			//removeChild('startForm');			requestChallenger();		}				private function loadGameData(){			// load the bout xml/web service		}				private function gameDataLoaded(){			// create the bout			for (var i=0; i<numberOfRounds; i++){							}		}				private function requestChallenger(){			// add requestChallengerForm					}				private function challengerSelected(){			// loadBout		}				private function loadBout(){			// display the bout			// add bout to the stage					}				private function requestPrediction(){					}				private function predictionSelected(){			// user has guessed			// we can now calculate			calculateResults();		}				private function calculateResults(){			// connect to web service			// get the answer					}													}	}